#include <stdio.h>

int main(void)
{
	size_t yang;
	size_t fri;
	size_t half;
	size_t yang_vol;
	size_t fri_vol;
	size_t res;

	res = 0;
	scanf("%zd %zd %zd %zd %zd", &yang, &fri, &half, &yang_vol, &fri_vol);
	if (yang > fri && yang > half && fri > half)
	{
		if (yang_vol > fri_vol)
			res = (2 *  * ) +
	}
}
